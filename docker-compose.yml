version: "3.9"

services:
  api:
    restart: always
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    volumes:
      - ./data:/go/src/app/data
    expose:
      - 3000

  web:
    restart: always
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    expose:
      - 3000

  reverse-proxy:
    image: nginx
    depends_on:
      - api
      - web
    restart: always
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./certs:/etc/ssl/certs
    ports:
      - 443:443
